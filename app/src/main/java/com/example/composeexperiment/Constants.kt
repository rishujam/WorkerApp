package com.example.composeexperiment

object Constants {

    const val FILTER_CHANNEL ="filter_channel"
    const val FILTER_CHANNEL_NAME ="Filter apply"
    const val ACTION_STOP =  "${BuildConfig.APPLICATION_ID}.stop"

    const val RES_PREFIX = "android.resource://${BuildConfig.APPLICATION_ID}/"
}